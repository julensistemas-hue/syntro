---
import BaseLayout from "../layouts/BaseLayout.astro";

const title = "Test IA Gratis: ¿Cuánto puede ahorrar tu empresa con automatización? | AI Security";
const description = "Descubre en 2 minutos cuántas horas estás perdiendo sin IA. Test gratuito de 12 preguntas con recomendaciones personalizadas. Sin registro.";
const canonical = "https://aisecurity.es/test-ia";
---

<BaseLayout title={title} description={description} canonical={canonical}>
  <!-- Hero Section -->
  <section class="relative py-16 overflow-hidden" style="background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);">
    <div class="relative z-10 max-w-4xl mx-auto px-6 md:px-8 text-center">
      <div class="inline-block px-4 py-2 bg-white/20 backdrop-blur-sm rounded-full text-white text-sm font-semibold mb-4">
        ✨ Test Gratuito • 2 minutos
      </div>
      <h1 class="text-3xl md:text-5xl font-bold text-white mb-4">
        ¿Está tu empresa<br/>aprovechando la IA?
      </h1>
      <p class="text-xl text-white/90 mb-6 max-w-2xl mx-auto">
        Responde 12 preguntas rápidas y descubre qué procesos podrías automatizar <strong>ahora mismo</strong>
      </p>
      <div  class="flex items-center justify-center gap-6 text-white/80 text-sm">
        <div class="flex items-center gap-2">
          <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 20 20">
            <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path>
          </svg>
          <span>Sin registro</span>
        </div>
        <div class="flex items-center gap-2">
          <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 20 20">
            <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path>
          </svg>
          <span>Resultados instantáneos</span>
        </div>
      </div>
    </div>
  </section>

  <!-- Quiz Container -->
  <section class="py-16 bg-base-50">
    <div class="max-w-3xl mx-auto px-6 md:px-8">

      <!-- Progress Bar -->
      <div class="mb-8">
        <div class="flex items-center justify-between text-sm text-base-600 mb-2">
          <span>Pregunta <span id="current-question">1</span> de <span id="total-questions">12</span></span>
          <span id="progress-percentage">8%</span>
        </div>
        <div  class="w-full bg-base-200 rounded-full h-3 overflow-hidden">
          <div id="progress-bar" class="h-full bg-gradient-to-r from-[#667eea] to-[#764ba2] transition-all duration-300" style="width: 8.33%;"></div>
        </div>
      </div>

      <!-- Question Cards -->
      <div  id="quiz-container">
        <!-- Questions will be injected here by JavaScript -->
      </div>

      <!-- Results Section (Hidden initially) -->
      <div  id="results-section" class="hidden">
        <!-- Score Overview -->
        <div class="bg-gradient-to-br from-[#667eea] to-[#764ba2] rounded-2xl p-8 text-white text-center mb-8">
          <div class="text-6xl font-bold mb-2" id="score-number">0</div>
          <div class="text-2xl font-semibold mb-2" id="score-label">Puntuación IA</div>
          <div class="text-lg opacity-90" id="score-subtitle">sobre 100 puntos</div>
        </div>

        <!-- Level Badge -->
        <div class="text-center mb-8">
          <div class="inline-block px-6 py-3 rounded-full text-lg font-bold" id="level-badge" style="background: white; color: #667eea;">
            <!-- Badge will be filled by JS -->
          </div>
        </div>

        <!-- Detailed Analysis -->
        <div class="space-y-6 mb-8">
          <h3 class="text-2xl font-bold text-base-900">📊 Análisis de tu empresa</h3>

          <!-- Category Scores -->
          <div id="category-scores" class="space-y-4">
            <!-- Categories will be injected here -->
          </div>
        </div>

        <!-- Recommendations -->
        <div style="background-color: #101113;" class="bg-blue-50 border-2 border-blue-200 rounded-xl p-6 mb-8">
          <h3 class="text-xl font-bold text-base-900 mb-4 flex items-center gap-2">
            <svg class="w-6 h-6 text-blue-600" fill="currentColor" viewBox="0 0 20 20">
              <path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z"></path>
            </svg>
            Recomendaciones para tu empresa
          </h3>
          <div id="recommendations-list" class="space-y-3">
            <!-- Recommendations will be injected here -->
          </div>
        </div>

        <!-- CTA Section -->
       

        <!-- Share Results -->
        <div class="text-center mt-8">
          <button
            id="restart-quiz"
            class="text-base-600 hover:text-base-900 font-semibold underline">
            🔄 Repetir el test
          </button>
        </div>
      </div>
    </div>
  </section>
</BaseLayout>

<script>
  // Quiz Questions Database
  const questions = [
    {
      id: 1,
      category: "Atención al Cliente",
      question: "¿Cómo gestionas las consultas de clientes actualmente?",
      options: [
        { text: "Solo por email/teléfono durante horario laboral", points: 0, icon: "📧" },
        { text: "Tenemos formulario web, pero sin respuestas automáticas", points: 3, icon: "📝" },
        { text: "Chatbot básico con respuestas predefinidas", points: 6, icon: "🤖" },
        { text: "IA que responde 24/7 y consulta bases de datos", points: 10, icon: "✨" }
      ],
      service: "chatbot"
    },
    {
      id: 2,
      category: "Gestión Documental",
      question: "¿Cómo encuentran información tus empleados en la documentación interna?",
      options: [
        { text: "Navegan manualmente por carpetas en red/drive", points: 0, icon: "📁" },
        { text: "Búsqueda por nombre de archivo", points: 2, icon: "🔍" },
        { text: "Buscador básico dentro de archivos", points: 5, icon: "📄" },
        { text: "Búsqueda semántica con IA (pregunta en lenguaje natural)", points: 10, icon: "🧠" }
      ],
      service: "gestor-documental"
    },
    {
      id: 3,
      category: "Gestión de Citas/Reservas",
      question: "¿Cómo gestionan las citas o reservas en tu empresa?",
      options: [
        { text: "No tenemos sistema de citas", points: 0, icon: "❌" },
        { text: "Por teléfono o email manualmente", points: 2, icon: "☎️" },
        { text: "Formulario web, luego confirmamos manualmente", points: 5, icon: "📋" },
        { text: "Sistema automatizado con IA, confirmaciones y recordatorios", points: 10, icon: "🗓️" }
      ],
      service: "gestor-citas"
    },
    {
      id: 4,
      category: "Atención Telefónica",
      question: "¿Quién atiende las llamadas entrantes?",
      options: [
        { text: "Empleados, cuando pueden contestar", points: 0, icon: "📞" },
        { text: "Recepcionista/secretaria en horario laboral", points: 3, icon: "👤" },
        { text: "Sistema IVR (menú automatizado básico)", points: 6, icon: "🎛️" },
        { text: "Asistente de IA con voz natural 24/7", points: 10, icon: "🎙️" }
      ],
      service: "atencion-llamadas"
    },
    {
      id: 5,
      category: "Automatización de Procesos",
      question: "¿Cuántas horas al mes dedicas a tareas repetitivas? (emails, informes, facturas...)",
      options: [
        { text: "Más de 40 horas/mes (sin automatización)", points: 0, icon: "😰" },
        { text: "20-40 horas/mes (algo automatizado)", points: 4, icon: "😓" },
        { text: "10-20 horas/mes (bastante automatizado)", points: 7, icon: "🙂" },
        { text: "Menos de 10 horas/mes (todo automatizado)", points: 10, icon: "🎉" }
      ],
      service: "automatizacion"
    },
    {
      id: 6,
      category: "Acceso a Datos en Tiempo Real",
      question: "¿Pueden tus empleados consultar datos (facturas, pedidos, stock) al instante?",
      options: [
        { text: "No, tienen que pedirlo a otros departamentos", points: 0, icon: "🚫" },
        { text: "Sí, pero solo con acceso directo al sistema (desktop)", points: 4, icon: "💻" },
        { text: "Sí, tienen app móvil del ERP/CRM", points: 7, icon: "📱" },
        { text: "Sí, pueden preguntar a chatbot/IA y obtener respuesta inmediata", points: 10, icon: "⚡" }
      ],
      service: "chatbot"
    },
    {
      id: 7,
      category: "Conocimiento de IA en el Equipo",
      question: "¿Tu equipo conoce y usa herramientas de IA (ChatGPT, Copilot...)?",
      options: [
        { text: "No, nadie las usa", points: 0, icon: "🤷" },
        { text: "Algunos las usan ocasionalmente por su cuenta", points: 4, icon: "👥" },
        { text: "Sí, las usamos de forma habitual", points: 7, icon: "✅" },
        { text: "Sí, y tenemos integraciones personalizadas para nuestros procesos", points: 10, icon: "🚀" }
      ],
      service: null
    },
    {
      id: 8,
      category: "Análisis de Datos",
      question: "¿Cómo analizas los datos de tu negocio (ventas, clientes, métricas)?",
      options: [
        { text: "No hacemos análisis de datos", points: 0, icon: "📉" },
        { text: "Revisamos datos manualmente en Excel", points: 3, icon: "📊" },
        { text: "Tenemos dashboards básicos", points: 6, icon: "📈" },
        { text: "IA analiza automáticamente y genera insights/alertas", points: 10, icon: "🧮" }
      ],
      service: "automatizacion"
    },
    {
      id: 9,
      category: "Formación de Empleados",
      question: "¿Cómo acceden tus empleados a manuales, procedimientos, políticas?",
      options: [
        { text: "Preguntan a compañeros o buscan en carpetas", points: 0, icon: "🤔" },
        { text: "Tenemos intranet/wiki, pero buscar es lento", points: 4, icon: "📚" },
        { text: "Sistema de búsqueda decente", points: 6, icon: "🔎" },
        { text: "Asistente IA responde dudas instantáneamente", points: 10, icon: "💡" }
      ],
      service: "gestor-documental"
    },
    {
      id: 10,
      category: "Generación de Contenido",
      question: "¿Usas IA para crear contenido (emails, informes, propuestas)?",
      options: [
        { text: "No, todo se escribe manualmente", points: 0, icon: "✍️" },
        { text: "Ocasionalmente copiamos de ChatGPT", points: 4, icon: "📝" },
        { text: "Usamos IA regularmente para borradores", points: 7, icon: "📄" },
        { text: "Tenemos plantillas y flujos automatizados con IA", points: 10, icon: "⚙️" }
      ],
      service: "automatizacion"
    },
    {
      id: 11,
      category: "Integración con Sistemas Existentes",
      question: "¿Tus herramientas de IA están integradas con tu ERP/CRM/bases de datos?",
      options: [
        { text: "No usamos IA", points: 0, icon: "❌" },
        { text: "Usamos IA, pero no está conectada a nuestros sistemas", points: 3, icon: "🔌" },
        { text: "Algunas integraciones básicas", points: 6, icon: "🔗" },
        { text: "Totalmente integrada, acceso en tiempo real", points: 10, icon: "🌐" }
      ],
      service: "chatbot"
    },
    {
      id: 12,
      category: "ROI y Medición",
      question: "¿Mides el tiempo/coste ahorrado con automatización?",
      options: [
        { text: "No medimos nada", points: 0, icon: "🙈" },
        { text: "Tenemos una idea aproximada", points: 3, icon: "🤷" },
        { text: "Hacemos estimaciones periódicas", points: 6, icon: "📊" },
        { text: "Dashboard con métricas precisas de ahorro", points: 10, icon: "💰" }
      ],
      service: "automatizacion"
    }
  ];

  // State
  let currentQuestionIndex = 0;
  let answers: { questionId: number; points: number; category: string; service: string | null }[] = [];

  // DOM Elements
  const quizContainer = document.getElementById('quiz-container') as HTMLDivElement;
  const resultsSection = document.getElementById('results-section') as HTMLDivElement;
  const progressBar = document.getElementById('progress-bar') as HTMLDivElement;
  const currentQuestionSpan = document.getElementById('current-question') as HTMLSpanElement;
  const totalQuestionsSpan = document.getElementById('total-questions') as HTMLSpanElement;
  const progressPercentage = document.getElementById('progress-percentage') as HTMLSpanElement;
  const restartButton = document.getElementById('restart-quiz') as HTMLButtonElement;

  // Initialize
  totalQuestionsSpan.textContent = questions.length.toString();
  renderQuestion();

  function renderQuestion() {
    const question = questions[currentQuestionIndex];

    quizContainer.innerHTML = `
      <div class="rounded-2xl shadow-xl p-8 border-2 border-base-200 animate-fade-in" style="background-color: black;">
        <div class="mb-2 text-sm font-semibold text-[#667eea] uppercase tracking-wide">
          ${question.category}
        </div>
        <h3 class="text-2xl md:text-3xl font-bold text-white mb-8">
          ${question.question}
        </h3>
        <div class="space-y-3">
          ${question.options.map((option, index) => `
            <button
              class="option-button w-full text-left p-5 rounded-xl border-2 border-base-200 hover:border-[#667eea] hover:bg-purple-50 transition-all duration-200 group"
              data-points="${option.points}"
              data-index="${index}">
              <div class="flex items-center gap-4">
                <div class="text-3xl group-hover:scale-110 transition-transform">
                  ${option.icon}
                </div>
                <div class="flex-1">
                  <div class="font-semibold text-base-900 group-hover:text-[#667eea] transition-colors">
                    ${option.text}
                  </div>
                </div>
                <div class="w-6 h-6 rounded-full border-2 border-base-300 group-hover:border-[#667eea] transition-colors flex items-center justify-center">
                  <div class="w-3 h-3 rounded-full bg-[#667eea] opacity-0 group-hover:opacity-100 transition-opacity"></div>
                </div>
              </div>
            </button>
          `).join('')}
        </div>
      </div>
    `;

    // Add click handlers
    const optionButtons = quizContainer.querySelectorAll('.option-button');
    optionButtons.forEach(button => {
      button.addEventListener('click', handleAnswer);
    });

    updateProgress();
  }

  function handleAnswer(e: Event) {
    const button = e.currentTarget as HTMLButtonElement;
    const points = parseInt(button.dataset.points || '0');
    const question = questions[currentQuestionIndex];

    // Save answer
    answers.push({
      questionId: question.id,
      points: points,
      category: question.category,
      service: question.service
    });

    // Visual feedback
    button.classList.add('bg-[#667eea]', 'border-[#667eea]', 'text-white');

    // Move to next question after brief delay
    setTimeout(() => {
      currentQuestionIndex++;

      if (currentQuestionIndex < questions.length) {
        renderQuestion();
      } else {
        showResults();
      }
    }, 300);
  }

  function updateProgress() {
    const progress = ((currentQuestionIndex + 1) / questions.length) * 100;
    progressBar.style.width = `${progress}%`;
    currentQuestionSpan.textContent = (currentQuestionIndex + 1).toString();
    progressPercentage.textContent = `${Math.round(progress)}%`;
  }

  function showResults() {
    // Calculate total score
    const totalPoints = answers.reduce((sum, answer) => sum + answer.points, 0);
    const maxPoints = questions.length * 10;
    const scorePercentage = Math.round((totalPoints / maxPoints) * 100);

    // Hide quiz, show results
    quizContainer.classList.add('hidden');
    resultsSection.classList.remove('hidden');

    // Animate score
    animateScore(scorePercentage);

    // Determine level
    const level = getLevel(scorePercentage);

    const levelBadge = document.getElementById('level-badge') as HTMLDivElement;
    levelBadge.textContent = level.label;
    levelBadge.style.background = level.color;
    levelBadge.style.color = 'white';

    const scoreLabel = document.getElementById('score-label') as HTMLDivElement;
    scoreLabel.textContent = level.title;

    const scoreSubtitle = document.getElementById('score-subtitle') as HTMLDivElement;
    scoreSubtitle.textContent = level.description;

    // Show category breakdown
    renderCategoryScores();

    // Show recommendations
    renderRecommendations(scorePercentage);

    // Scroll to results
    resultsSection.scrollIntoView({ behavior: 'smooth', block: 'start' });
  }

  function animateScore(targetScore: number) {
    const scoreElement = document.getElementById('score-number') as HTMLDivElement;
    let currentScore = 0;
    const increment = Math.ceil(targetScore / 50);

    const interval = setInterval(() => {
      currentScore += increment;
      if (currentScore >= targetScore) {
        currentScore = targetScore;
        clearInterval(interval);
      }
      scoreElement.textContent = currentScore.toString();
    }, 30);
  }

  function getLevel(score: number) {
    if (score >= 80) {
      return {
        label: '🚀 Nivel Avanzado',
        title: '¡Empresa líder en IA!',
        description: 'Tu empresa está en la vanguardia de la automatización',
        color: 'linear-gradient(135deg, #11998e 0%, #38ef7d 100%)'
      };
    } else if (score >= 60) {
      return {
        label: '📈 Nivel Intermedio',
        title: 'Buen camino hacia la automatización',
        description: 'Tienes base sólida, con margen de mejora',
        color: 'linear-gradient(135deg, #667eea 0%, #764ba2 100%)'
      };
    } else if (score >= 30) {
      return {
        label: '🌱 Nivel Inicial',
        title: 'Comenzando con IA',
        description: 'Grandes oportunidades de automatización',
        color: 'linear-gradient(135deg, #f093fb 0%, #f5576c 100%)'
      };
    } else {
      return {
        label: '💡 Sin IA',
        title: 'Potencial sin explotar',
        description: 'Podrías ahorrar cientos de horas con IA',
        color: 'linear-gradient(135deg, #fa709a 0%, #fee140 100%)'
      };
    }
  }

  function renderCategoryScores() {
    const categoryScoresDiv = document.getElementById('category-scores') as HTMLDivElement;

    // Group answers by category
    const categoryMap = new Map<string, number[]>();
    answers.forEach(answer => {
      if (!categoryMap.has(answer.category)) {
        categoryMap.set(answer.category, []);
      }
      categoryMap.get(answer.category)!.push(answer.points);
    });

    // Calculate average for each category
    const categoryScores = Array.from(categoryMap.entries()).map(([category, points]) => {
      const avg = points.reduce((sum, p) => sum + p, 0) / points.length;
      const percentage = Math.round((avg / 10) * 100);
      return { category, percentage };
    });

    categoryScoresDiv.innerHTML = categoryScores.map(cat => `
      <div>
        <div class="flex items-center justify-between mb-2">
          <span class="font-semibold text-base-800">${cat.category}</span>
          <span class="text-sm font-bold text-[#667eea]">${cat.percentage}%</span>
        </div>
        <div class="w-full bg-base-200 rounded-full h-3">
          <div
            class="h-full rounded-full transition-all duration-1000"
            style="width: ${cat.percentage}%; background: linear-gradient(90deg, #667eea 0%, #764ba2 100%);">
          </div>
        </div>
      </div>
    `).join('');
  }

  function renderRecommendations(score: number) {
    const recommendationsDiv = document.getElementById('recommendations-list') as HTMLDivElement;

    // Count services mentioned
    const serviceCounts = new Map<string, number>();
    answers.forEach(answer => {
      if (answer.service && answer.points < 7) { // Only low-scoring answers
        serviceCounts.set(answer.service, (serviceCounts.get(answer.service) || 0) + 1);
      }
    });

    // Sort by most needed
    const sortedServices = Array.from(serviceCounts.entries())
      .sort((a, b) => b[1] - a[1])
      .slice(0, 3);

    const serviceInfo: Record<string, { title: string; benefit: string; url: string }> = {
      'chatbot': {
        title: '🤖 Chatbot Inteligente 24/7',
        benefit: 'Reduce un 70% las consultas repetitivas y atiende clientes mientras duermes',
        url: '/servicios/chatbot'
      },
      'gestor-documental': {
        title: '📚 Gestor Documental con IA',
        benefit: 'Encuentra cualquier documento en segundos, no en 15 minutos',
        url: '/servicios/gestor-documental'
      },
      'gestor-citas': {
        title: '🗓️ Automatización de Citas',
        benefit: 'Elimina llamadas para agendar, confirmar y recordar citas',
        url: '/servicios/gestor-citas'
      },
      'atencion-llamadas': {
        title: '📞 Recepcionista Virtual IA',
        benefit: 'Atiende llamadas 24/7 con voz natural en español',
        url: '/servicios/atencion-llamadas'
      },
      'automatizacion': {
        title: '⚙️ Automatización de Procesos RPA',
        benefit: 'Libera 20+ horas/mes automatizando emails, informes y tareas repetitivas',
        url: '/servicios/automatizacion'
      }
    };

    if (sortedServices.length === 0) {
      recommendationsDiv.innerHTML = `
        <div style="background-color: black;" class="text-base-700">
          ✅ ¡Excelente! Tu empresa ya tiene un nivel alto de automatización.
          <a href="/reunion" class="text-[#667eea] font-semibold underline">Contáctanos</a>
          para optimizar aún más tus procesos.
        </div>
      `;
    } else {
      recommendationsDiv.innerHTML = sortedServices.map(([service, _]) => {
        const info = serviceInfo[service];
        return `
          <div style="background-color: black;" class="flex items-start gap-3 p-4 bg-white rounded-lg border border-blue-200">
            <div class="flex-1">
              <div class="font-bold text-base-900 mb-1">${info.title}</div>
              <div class="text-sm text-base-600 mb-2">${info.benefit}</div>
              <a href="${info.url}" class="text-sm text-[#667eea] font-semibold hover:underline">
                Ver demo interactiva →
              </a>
            </div>
          </div>
        `;
      }).join('');
    }
  }

  // Restart quiz
  restartButton?.addEventListener('click', () => {
    currentQuestionIndex = 0;
    answers = [];
    quizContainer.classList.remove('hidden');
    resultsSection.classList.add('hidden');
    renderQuestion();
    window.scrollTo({ top: 0, behavior: 'smooth' });
  });
</script>

<style>
  @keyframes fade-in {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .animate-fade-in {
    animation: fade-in 0.4s ease-out;
  }

  .option-button:active {
    transform: scale(0.98);
  }
</style>
</BaseLayout>
