---
import BaseLayout from "../../../layouts/BaseLayout.astro";

const title = "Technical Blog — Wazuh, SIEM & AI for IT | AI Security";
const description = "Practical guides on Wazuh, SIEM and AI applied to IT systems and cybersecurity. Real configurations and hands-on tutorials, no unnecessary theory.";
const canonical = "https://aisecurity.es/en/blog";

// Array of blog articles (English) — Wazuh cluster
const articles = [
  {
    slug: "que-es-wazuh-para-que-sirve",
    title: "What Is Wazuh and What Is It Used For: Open Source SIEM/XDR [2026]",
    description: "What Wazuh is, what it's used for and how it protects your business: threat detection, compliance, log monitoring on Linux and Windows. Guide with real examples.",
    publishDate: "2026-02-12",
    readTime: "10 min",
    tags: ["Wazuh", "SIEM", "XDR", "Cybersecurity", "Open Source", "Monitoring"],
    category: "Wazuh & Security"
  },
  {
    slug: "como-instalar-wazuh-en-linux",
    title: "How to Install Wazuh on Linux (Ubuntu 24.04)",
    description: "Learn how to install Wazuh on Ubuntu, Debian, CentOS and Rocky Linux. Step-by-step guide with the all-in-one method and separate component install.",
    publishDate: "2026-02-18",
    readTime: "18 min",
    tags: ["Wazuh", "Linux", "Ubuntu", "Installation", "SIEM", "Cybersecurity"],
    category: "Wazuh & Security"
  },
  {
    slug: "como-instalar-wazuh-con-docker",
    title: "How to Install Wazuh with Docker Compose",
    description: "Step-by-step guide to install Wazuh using Docker Compose. Deploy the full SIEM in minutes with containers: Indexer, Server and Dashboard up and running.",
    publishDate: "2026-02-25",
    readTime: "12 min",
    tags: ["Wazuh", "Docker", "Docker Compose", "Containers", "SIEM", "Cybersecurity"],
    category: "Wazuh & Security"
  },
  {
    slug: "como-instalar-agente-wazuh-en-windows",
    title: "Install Wazuh Agent on Windows: Complete Guide 2026",
    description: "How to install the Wazuh agent on Windows step by step: MSI download, install command, ossec.conf configuration, localfile and syscheck. Ready in 15 minutes.",
    publishDate: "2026-02-25",
    readTime: "10 min",
    tags: ["Wazuh", "Windows", "Agent", "SIEM", "Monitoring", "Cybersecurity"],
    category: "Wazuh & Security"
  },
  {
    slug: "como-instalar-agente-wazuh-en-linux",
    title: "How to Install the Wazuh Agent on Linux",
    description: "Step-by-step guide to install and configure the Wazuh agent on Linux (Debian/Ubuntu). Install command, ossec.conf setup, localfile and restarting the service.",
    publishDate: "2026-02-25",
    readTime: "8 min",
    tags: ["Wazuh", "Linux", "Agent", "SIEM", "Monitoring", "Cybersecurity"],
    category: "Wazuh & Security"
  },
  {
    slug: "configuracion-de-grupos-en-wazuh",
    title: "How to Configure Agent Groups in Wazuh",
    description: "Learn to organize your agents into groups (Windows, Linux, databases) and push centralized ossec.conf configurations to every agent in the group.",
    publishDate: "2026-02-26",
    readTime: "10 min",
    tags: ["Wazuh", "Groups", "Configuration", "SIEM", "Management", "Cybersecurity"],
    category: "Wazuh & Security"
  },
  {
    slug: "configurar-equipos-sin-agente-wazuh-syslog",
    title: "Monitor Agentless Devices in Wazuh with Syslog",
    description: "Monitor routers, switches, NAS and agentless devices using rsyslog as a collector. Complete setup with templates, filters and Wazuh integration.",
    publishDate: "2026-03-08",
    readTime: "8 min",
    tags: ["Wazuh", "Syslog", "rsyslog", "Agentless", "SIEM", "Networking"],
    category: "Wazuh & Security"
  },
  {
    slug: "flujo-logs-wazuh-decoders-reglas-alertas",
    title: "Wazuh Log Flow: Decoders, Rules and Alerts",
    description: "Understand how Wazuh processes logs: pre-decoder, decoder, rules and alerts. Configure notifications by email, Discord or Slack.",
    publishDate: "2026-03-04",
    readTime: "12 min",
    tags: ["Wazuh", "Decoders", "Rules", "Alerts", "SIEM", "Logs"],
    category: "Wazuh & Security"
  },
  {
    slug: "crear-reglas-personalizadas-wazuh",
    title: "How to Create Custom Rules in Wazuh: Detect Access and Actions",
    description: "Learn how to write custom Wazuh rules to detect folder access, specific users or Delete/Write actions. Practical examples with NAS logs and dynamic variables.",
    publishDate: "2026-03-06",
    readTime: "10 min",
    tags: ["Wazuh", "Rules", "Alerts", "NAS", "SIEM", "Cybersecurity"],
    category: "Wazuh & Security"
  },
  {
    slug: "configurar-alertas-correo-wazuh-postfix-gmail",
    title: "How to Configure Email Alerts in Wazuh with Postfix and Gmail/Microsoft",
    description: "Set up email alerts in Wazuh with Postfix and Gmail/Microsoft 365. Covers ISO 27001 (A.5.24-A.5.26) and ENS (op.mon.1, op.mon.3) compliance.",
    publishDate: "2026-03-06",
    readTime: "12 min",
    tags: ["Wazuh", "Alerts", "Postfix", "Gmail", "SMTP", "Email", "ISO 27001"],
    category: "Wazuh & Security"
  },
  {
    slug: "detectar-instalaciones-software-windows-wazuh",
    title: "How to Detect Software Installs and Uninstalls in Windows with Wazuh",
    description: "Create Wazuh rules to detect software installs and uninstalls in Windows (Event IDs 11707 and 11724). ISO 27001 and compliance auditing included.",
    publishDate: "2026-03-06",
    readTime: "8 min",
    tags: ["Wazuh", "Windows", "Rules", "Software", "Auditing", "SIEM", "ISO 27001"],
    category: "Wazuh & Security"
  },
  {
    slug: "monitorizar-certificados-wazuh-fim",
    title: "Monitor Digital Certificates with Wazuh FIM (ISO 27001 and ENS)",
    description: "Hands-on lab: configure FIM to detect changes to .pfx, .p12 and .cer certificates. Meet ISO 27001 A.8.24 and ENS mp.com.3 with custom alerts.",
    publishDate: "2026-03-03",
    readTime: "10 min",
    tags: ["Wazuh", "FIM", "Certificates", "ISO 27001", "Compliance", "Cybersecurity"],
    category: "Wazuh & Security"
  },
  {
    slug: "integracion-wazuh-virustotal",
    title: "Wazuh and VirusTotal Integration: Automated Malware Analysis",
    description: "Set up Wazuh to automatically scan suspicious files against 70+ antivirus engines with the VirusTotal API. Step-by-step guide using FIM and EICAR.",
    publishDate: "2026-03-03",
    readTime: "8 min",
    tags: ["Wazuh", "VirusTotal", "Malware", "FIM", "SIEM", "Integrations"],
    category: "Wazuh & Security"
  },
  {
    slug: "panel-vulnerability-detection-wazuh",
    title: "Wazuh Vulnerability Detection: Meet ISO 27001 and ENS",
    description: "Configure Wazuh's vulnerability detector to identify CVEs across your infrastructure. Meet ISO 27001 A.8.8 and ENS op.exp.6 with continuous scanning.",
    publishDate: "2026-03-04",
    readTime: "10 min",
    tags: ["Wazuh", "Vulnerabilities", "CVE", "ISO 27001", "Compliance", "Cybersecurity"],
    category: "Wazuh & Security"
  },
  {
    slug: "investigar-ataque-ssh-wazuh-discover",
    title: "Case Study: Investigating an SSH Attack with Wazuh Discover and Threat Hunting",
    description: "Investigate a real SSH brute-force attack step by step using Wazuh's Threat Hunting and Discover panels. A hands-on case study with remediation measures.",
    publishDate: "2026-03-06",
    readTime: "12 min",
    tags: ["Wazuh", "Discover", "Threat Hunting", "SSH", "Incidents", "Forensics"],
    category: "Wazuh & Security"
  },
  {
    slug: "monitorizacion-de-contenedores-docker-con-wazuh",
    title: "How to Monitor a Docker Server and Its Containers with Wazuh",
    description: "Configure Wazuh to monitor the Docker host and its containers: Docker Listener, container logs, FIM, real alerts and suspicious docker exec detection.",
    publishDate: "2026-01-10",
    readTime: "12 min",
    tags: ["Docker", "Containers", "Wazuh", "Monitoring", "FIM", "Logs", "Cybersecurity"],
    category: "Wazuh & Security"
  },
  {
    slug: "wazuh-elastic-security-configuracion-de-siem-completo",
    title: "Wazuh + Elastic Security: Complete SIEM Setup Guide",
    description: "Build a complete SIEM by integrating Wazuh with Elastic Security. Step-by-step install of Elasticsearch, Kibana and Filebeat plus real-time alert dashboards.",
    publishDate: "2025-12-22",
    readTime: "8 min",
    tags: ["Elasticsearch", "Kibana", "SIEM", "Dashboards", "Wazuh", "Cybersecurity"],
    category: "Wazuh & Security"
  },
  {
    slug: "como-monitorizar-tu-tenant-de-office365-registros-de-exchange-sharepoint",
    title: "How to Monitor Your Office 365 Tenant, Exchange & SharePoint Logs",
    description: "Monitor your Office 365 tenant with Wazuh: ingest Exchange and SharePoint logs and build rules to detect foreign logins, brute force and phishing-driven breaches.",
    publishDate: "2025-12-15",
    readTime: "8 min",
    tags: ["Office 365", "Exchange", "SharePoint", "Phishing", "Alerts", "Wazuh", "Cybersecurity"],
    category: "Wazuh & Security"
  },
  {
    slug: "wazuh-mcp-server-claude-ia",
    title: "Wazuh MCP Server: Query and Act on Your SIEM with Claude AI",
    description: "Connect Wazuh to Claude with MCP. Ask about alerts and vulnerabilities in natural language and run actions like isolating servers or killing processes.",
    publishDate: "2026-05-20",
    readTime: "10 min",
    tags: ["Wazuh", "Claude", "MCP", "AI", "SIEM", "Integrations"],
    category: "Wazuh & Security"
  },
  {
    slug: "ia-threat-hunting-wazuh",
    title: "AI Threat Hunting in Wazuh: Installation Guide with Ollama",
    description: "Integrate AI into Wazuh for threat hunting. Install Ollama with a local LLM, connect it to your Wazuh logs and query security events in natural language, on-prem.",
    publishDate: "2025-12-02",
    readTime: "8 min",
    tags: ["Wazuh", "Artificial Intelligence", "Cybersecurity", "Threat Hunting"],
    category: "Wazuh & Security"
  },
].sort((a, b) => new Date(b.publishDate).getTime() - new Date(a.publishDate).getTime());

// Available categories
const categories = ["All", "Awareness", "Wazuh & Security", "AI for IT"];

const hasArticles = articles.length > 0;
---

<BaseLayout title={title} description={description} canonical={canonical}>
  <!-- Hero Section -->
  <section class="py-16 md:py-20 relative overflow-hidden" style="background:#050f1f;border-bottom:1px solid rgba(255,255,255,0.08);">
    <!-- Particle animation canvas -->
    <canvas id="blog-hero-canvas" class="absolute inset-0 w-full h-full" style="opacity:0.5;"></canvas>
    <!-- Top/bottom gradient to blend with the rest -->
    <div class="absolute inset-x-0 bottom-0 h-16" style="background:linear-gradient(to bottom,transparent,#020817);"></div>

    <div class="relative z-10 max-w-7xl mx-auto px-6 md:px-12">
      <div class="max-w-3xl">
        <div class="inline-flex items-center gap-2 px-4 py-2 bg-white/10 backdrop-blur-sm text-white rounded-full text-sm font-semibold border border-white/20 mb-6">
          <span class="text-xl">📰</span>
          Technical blog
        </div>

        <h1 class="text-4xl md:text-5xl lg:text-6xl font-bold text-white mb-6">
          News & Tutorials
        </h1>

        <p class="text-xl text-white/80 leading-relaxed">
          Technical articles on <strong class="text-white">AI applied to IT systems</strong> and <strong class="text-white">cybersecurity with Wazuh</strong>.
          Practical guides, real configurations and use cases without unnecessary theory.
        </p>
      </div>
    </div>
  </section>

  <script is:inline>
  (function() {
    const canvas = document.getElementById('blog-hero-canvas');
    if (!canvas) return;
    const ctx = canvas.getContext('2d');

    function resize() {
      canvas.width = canvas.offsetWidth;
      canvas.height = canvas.offsetHeight;
    }
    resize();
    window.addEventListener('resize', resize);

    const NODES = 60;
    const MAX_DIST = 140;
    const nodes = Array.from({ length: NODES }, () => ({
      x: Math.random() * canvas.width,
      y: Math.random() * canvas.height,
      vx: (Math.random() - 0.5) * 0.4,
      vy: (Math.random() - 0.5) * 0.4,
      r: Math.random() * 1.5 + 1,
      // pulse phase
      phase: Math.random() * Math.PI * 2,
    }));

    function draw() {
      ctx.clearRect(0, 0, canvas.width, canvas.height);

      // Update
      nodes.forEach(n => {
        n.x += n.vx;
        n.y += n.vy;
        n.phase += 0.02;
        if (n.x < 0 || n.x > canvas.width) n.vx *= -1;
        if (n.y < 0 || n.y > canvas.height) n.vy *= -1;
      });

      // Lines
      for (let i = 0; i < NODES; i++) {
        for (let j = i + 1; j < NODES; j++) {
          const dx = nodes[i].x - nodes[j].x;
          const dy = nodes[i].y - nodes[j].y;
          const dist = Math.sqrt(dx * dx + dy * dy);
          if (dist < MAX_DIST) {
            const alpha = (1 - dist / MAX_DIST) * 0.25;
            ctx.beginPath();
            ctx.moveTo(nodes[i].x, nodes[i].y);
            ctx.lineTo(nodes[j].x, nodes[j].y);
            ctx.strokeStyle = `rgba(96,165,250,${alpha})`;
            ctx.lineWidth = 0.8;
            ctx.stroke();
          }
        }
      }

      // Nodes
      nodes.forEach(n => {
        const pulse = 0.5 + 0.5 * Math.sin(n.phase);
        ctx.beginPath();
        ctx.arc(n.x, n.y, n.r + pulse * 0.8, 0, Math.PI * 2);
        ctx.fillStyle = `rgba(129,140,248,${0.4 + pulse * 0.4})`;
        ctx.fill();
      });

      requestAnimationFrame(draw);
    }
    draw();
  })();
  </script>

  {hasArticles && (
    <!-- Categories Filter -->
    <section class="py-6 border-b sticky top-[73px] z-40" style="background:#020817;border-color:rgba(255,255,255,0.08)">
      <div class="max-w-7xl mx-auto px-6 md:px-12">
        <div class="flex flex-wrap gap-2">
          {categories.map((category, index) => (
            <button
              data-category={category}
              class={`px-4 py-1.5 rounded-full font-medium text-sm transition-all duration-200 ${
                index === 0
                  ? 'bg-accent-600 text-white'
                  : 'text-white/60 border hover:text-white hover:border-white/30'
              }`}
              style={index !== 0 ? 'border-color:rgba(255,255,255,0.15);background:rgba(255,255,255,0.05)' : ''}>
              {category}
            </button>
          ))}
        </div>
      </div>
    </section>
  )}

  <!-- Articles Grid -->
  <section class="py-16 md:py-20 relative overflow-hidden" style="background:#020817;">
    <div class="relative z-10 max-w-7xl mx-auto px-6 md:px-12">
      {hasArticles ? (
        <>
          <!-- Stats -->
          <div class="mb-12 flex items-center justify-between">
            <p id="article-count" class="text-white/70">
              <span class="font-semibold text-white">{articles.length}</span> articles published
            </p>
          </div>

          <!-- Articles List -->
          <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
            {articles.map(article => (
              <article
                class="blog-card group flex flex-col rounded-xl border border-white/10 overflow-hidden transition-all duration-300 hover:-translate-y-1 hover:border-white/20 hover:shadow-xl hover:shadow-black/40 cursor-pointer"
                style="background:rgba(255,255,255,0.04);"
                data-article-category={article.category}
                onclick={`window.location.href='/en/blog/${article.slug}'`}>

                <!-- Top accent line by category -->
                <div class="h-0.5 w-full" style={
                  article.category === 'Wazuh & Security' ? 'background:linear-gradient(90deg,#60a5fa,#818cf8)' :
                  article.category === 'Awareness' ? 'background:linear-gradient(90deg,#f59e0b,#f97316)' :
                  article.category === 'Tech Support' ? 'background:linear-gradient(90deg,#10b981,#14b8a6)' :
                  'background:linear-gradient(90deg,#a78bfa,#60a5fa)'
                }></div>

                <div class="p-5 flex flex-col flex-1">
                  <!-- Category + read time row -->
                  <div class="flex items-center justify-between mb-3">
                    <span class="text-xs font-semibold px-2.5 py-1 rounded-full" style={
                      article.category === 'Wazuh & Security' ? 'background:rgba(96,165,250,0.15);color:#93c5fd;' :
                      article.category === 'Awareness' ? 'background:rgba(245,158,11,0.15);color:#fcd34d;' :
                      article.category === 'Tech Support' ? 'background:rgba(16,185,129,0.15);color:#6ee7b7;' :
                      'background:rgba(167,139,250,0.15);color:#c4b5fd;'
                    }>
                      {article.category}
                    </span>
                    <span class="flex items-center gap-1 text-xs text-white/40">
                      <svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                      </svg>
                      {article.readTime}
                    </span>
                  </div>

                  <!-- Title -->
                  <a href={`/en/blog/${article.slug}`} class="block mb-2 group-hover:text-accent-300 transition-colors">
                    <p class="font-bold text-white leading-snug" style="font-size:1rem;">
                      {article.title}
                    </p>
                  </a>

                  <!-- Description -->
                  <p class="text-white/55 text-sm leading-relaxed mb-4 flex-1" style="font-size:0.8rem;">
                    {article.description}
                  </p>

                  <!-- Tags -->
                  <div class="flex flex-wrap gap-1.5 mb-4">
                    {article.tags.slice(0, 3).map(tag => (
                      <span class="text-xs text-white/40 bg-white/5 px-2 py-0.5 rounded border border-white/10">
                        {tag}
                      </span>
                    ))}
                  </div>

                  <!-- Footer -->
                  <div class="pt-3 border-t border-white/10">
                    <time class="text-xs text-white/35" datetime={article.publishDate}>
                      {new Date(article.publishDate).toLocaleDateString('en-US', {
                        year: 'numeric',
                        month: 'short',
                        day: 'numeric'
                      })}
                    </time>
                  </div>
                </div>
              </article>
            ))}
          </div>

          <!-- Empty State (when no articles match the filter) -->
          <div id="empty-state" class="hidden text-center py-16">
            <div class="text-6xl mb-4">📭</div>
            <h3 class="text-xl font-semibold text-white mb-2">No articles in this category</h3>
            <p class="text-white/70">Try another category or come back later</p>
          </div>
        </>
      ) : (
        <!-- No translated articles yet -->
        <div class="max-w-2xl mx-auto text-center py-12 md:py-16">
          <div class="text-6xl mb-6">🌐</div>
          <h2 class="text-2xl md:text-3xl font-bold text-white mb-4">
            English articles are on the way
          </h2>
          <p class="text-lg text-white/70 leading-relaxed mb-10">
            We're translating our technical articles into English. New guides on
            Wazuh, SIEM and IT security are coming soon.
          </p>

          <div class="flex flex-col sm:flex-row items-center justify-center gap-4">
            <a
              href="/en/wazuh"
              class="inline-flex items-center justify-center px-6 py-3 rounded-lg font-semibold text-white transition-all duration-200 hover:-translate-y-0.5"
              style="background:#2563eb;">
              Explore Wazuh
            </a>
            <a
              href="/blog"
              class="inline-flex items-center gap-1 text-white/70 hover:text-white font-medium transition-colors">
              Read our Spanish articles →
            </a>
          </div>
        </div>
      )}
    </div>
  </section>
</BaseLayout>

<script>
  // Category filter (only active when articles are rendered)
  document.addEventListener('DOMContentLoaded', () => {
    const categoryButtons = document.querySelectorAll('[data-category]');
    const articles = document.querySelectorAll('[data-article-category]');
    const emptyState = document.getElementById('empty-state');
    const articleCount = document.getElementById('article-count');

    if (categoryButtons.length === 0) return;

    // Update the counter
    const updateCount = (count: number) => {
      if (articleCount) {
        articleCount.innerHTML = `<span class="font-semibold text-white">${count}</span> ${count === 1 ? 'article published' : 'articles published'}`;
      }
    };

    categoryButtons.forEach(button => {
      button.addEventListener('click', () => {
        const selectedCategory = button.getAttribute('data-category');

        // Update button styles
        categoryButtons.forEach(btn => {
          if (btn === button) {
            btn.classList.add('bg-accent-600', 'text-white');
            btn.classList.remove('text-white/60', 'border', 'hover:text-white', 'hover:border-white/30');
            (btn as HTMLElement).style.borderColor = '';
            (btn as HTMLElement).style.background = '';
          } else {
            btn.classList.remove('bg-accent-600', 'text-white');
            btn.classList.add('text-white/60', 'border', 'hover:text-white', 'hover:border-white/30');
            (btn as HTMLElement).style.borderColor = 'rgba(255,255,255,0.15)';
            (btn as HTMLElement).style.background = 'rgba(255,255,255,0.05)';
          }
        });

        // Filter articles
        let visibleCount = 0;
        articles.forEach(article => {
          const articleCategory = article.getAttribute('data-article-category');
          if (selectedCategory === 'All' || articleCategory === selectedCategory) {
            article.classList.remove('hidden');
            visibleCount++;
          } else {
            article.classList.add('hidden');
          }
        });

        // Update counter
        updateCount(visibleCount);

        // Show/hide empty state
        if (visibleCount === 0) {
          emptyState?.classList.remove('hidden');
        } else {
          emptyState?.classList.add('hidden');
        }
      });
    });
  });
</script>
