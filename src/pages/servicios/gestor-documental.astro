---
import BaseLayout from "../../layouts/BaseLayout.astro";
---

<BaseLayout>
  <section class="py-20 bg-base-25">
    <div class="max-w-7xl mx-auto px-8 md:px-12 lg:px-24">
      <!-- Hero Section -->
      <div class="text-center mb-16">
       
        <h1 class="text-4xl md:text-5xl font-bold text-base-900 mb-6">
          Encuentra cualquier documento <span class="text-accent-600">en segundos</span>
        </h1>
      </div>

      <!-- Interactive Demo Section -->
      <div class="mb-16">
        <div class="max-w-4xl mx-auto bg-gradient-to-br from-base-900 to-base-800 rounded-2xl shadow-2xl overflow-hidden">
          <!-- Demo Header -->
          <div class="bg-base-800 px-6 py-4 border-b border-base-700">
            <div class="flex items-center justify-between">
              <div class="flex items-center gap-3">
                <div class="w-3 h-3 rounded-full bg-red-500"></div>
                <div class="w-3 h-3 rounded-full bg-yellow-500"></div>
                <div class="w-3 h-3 rounded-full bg-green-500"></div>
                <span class="ml-4 text-base-300 text-sm font-medium">Gestor Documental - Demo</span>
              </div>
              <div class="flex items-center gap-2 text-base-400 text-sm">
                <span>📚 1,247 documentos indexados</span>
              </div>
            </div>
          </div>

          <!-- Demo Content -->
          <div class="bg-gradient-to-br from-blue-500 to-blue-700 p-8">
            <div class="max-w-3xl mx-auto">
              <!-- Search Screen -->
              <div class="rounded-3xl p-6 shadow-2xl" style="background: linear-gradient(135deg, #0f172a 0%, #1e293b 100%);">
                <!-- Search Bar -->
                <div class="mb-6">
                  <form id="doc-search-form" class="relative">
                    <input
                      type="text"
                      id="doc-search-input"
                      placeholder="Escribe tu pregunta..."
                      class="w-full px-6 py-4 pr-24 border-2 border-blue-400/40 rounded-xl focus:outline-none focus:border-blue-400 placeholder-base-400"
                      style="background: rgba(255, 255, 255, 1); color: #000000;"
                    />
                    <button
                      type="submit"
                      style="height: 100%"
                      class="absolute right-0 px-6 py-2 bg-blue-600 text-white rounded-lg font-semibold hover:bg-blue-700 transition-all duration-200"
                    >
                      Buscar
                    </button>
                  </form>
                </div>

                <!-- Quick Actions -->
                <div class="mb-6">
                  <p class="text-sm text-white mb-3 font-medium">Prueba estas búsquedas:</p>
                  <div class="flex flex-wrap gap-2">
                    <button
                      class="quick-search-btn px-4 py-2 bg-white hover:bg-blue-50 border-2 border-blue-300 hover:border-blue-400 rounded-lg text-sm font-medium transition-all duration-200 shadow-md"
                      data-query="¿Cuál es la política de teletrabajo?"
                      style="color: #000000;">
                      📱 Política de teletrabajo
                    </button>
                    <button
                      class="quick-search-btn px-4 py-2 bg-white hover:bg-blue-50 border-2 border-blue-300 hover:border-blue-400 rounded-lg text-sm font-medium transition-all duration-200 shadow-md"
                      data-query="¿Cuáles son los requisitos para solicitar vacaciones?"
                      style="color: #000000;">
                      🏖️ Requisitos para vacaciones
                    </button>
                    <button
                      class="quick-search-btn px-4 py-2 bg-white hover:bg-blue-50 border-2 border-blue-300 hover:border-blue-400 rounded-lg text-sm font-medium transition-all duration-200 shadow-md"
                      data-query="¿Cómo funciona el procedimiento de compras?"
                      style="color: #000000;">
                      🛒 Procedimiento de compras
                    </button>
                  </div>
                </div>

                <!-- Results Container -->
                <div id="doc-results" class="space-y-4 min-h-[400px]">
                  <!-- Initial State -->
                  <div class="text-center py-16 text-white/70">
                    <div class="text-6xl mb-4">🔍</div>
                    <p class="text-lg font-medium mx-auto max-w-md">Haz clic en una búsqueda rápida o escribe tu pregunta</p>
                    <p class="text-sm mt-2 mx-auto max-w-md">El sistema buscará en toda la documentación y extraerá la información relevante</p>
                  </div>
                </div>
              </div>
            </div>
          </div>

          <!-- Info Panel -->
               <div class="bg-base-100 px-6 py-5">
            <p class="text-sm text-base-600 text-center font-medium max-w-2xl mx-auto">
              Esta es una demo simulada. El sistema real puede tardar unos segundos más.
            </p>
          </div>
        </div>
      </div>

       <p class="text-xl text-base-600 max-w-3xl mx-auto mb-8 text-center">
        Busca información en miles de documentos usando lenguaje natural.
        El sistema entiende tu pregunta y encuentra la respuesta exacta.
      </p>
 <div class="max-w-4xl mx-auto mb-16">
        <div class="rounded-2xl shadow-2xl overflow-hidden border-2 border-base-400 bg-black">
          <div class="p-6 text-white">
            <h3 class="text-sm font-semibold mb-4">📋 Contexto de la demo:</h3>
            <p class="text-sm mb-6 opacity-90">
              Esta es una simulación para una empresa de consultoría con 50 empleados.
              El sistema tiene indexados más de 1,200 documentos internos: políticas, procedimientos,
              manuales y normativas.
            </p>

            <!-- Before/After Comparison Cards -->
            <div class="grid grid-cols-1 md:grid-cols-2 gap-4 mt-6">
              <!-- Before Card -->
              <div class="bg-white/5 backdrop-blur-sm rounded-lg p-5 border-2 border-red-500 hover:border-red-400 transition-all duration-300 shadow-lg shadow-red-500/20">
                <div class="flex items-center gap-2 mb-3">
                  <div class="w-8 h-8 bg-red-500/40 rounded-lg flex items-center justify-center">
                    <span class="text-xl">😓</span>
                  </div>
                  <h4 class="font-bold text-white">Antes</h4>
                </div>
                <p class="text-sm text-white/90 leading-relaxed">
                  Buscar información llevaba <strong class="text-red-400">10-15 minutos</strong> navegando entre carpetas, archivos y versiones obsoletas.
                </p>
              </div>

              <!-- After Card -->
              <div class="bg-white/5 backdrop-blur-sm rounded-lg p-5 border-2 border-green-500 hover:border-green-400 transition-all duration-300 shadow-lg shadow-green-500/20">
                <div class="flex items-center gap-2 mb-3">
                  <div class="w-8 h-8 bg-green-500/40 rounded-lg flex items-center justify-center">
                    <span class="text-xl">⚡</span>
                  </div>
                  <h4 class="font-bold text-white">Ahora</h4>
                </div>
                <p class="text-sm text-white/90 leading-relaxed">
                  Respuesta en <strong class="text-green-300">&lt;1-5 segundos</strong> con el extracto exacto del documento y su ubicación.
                </p>
              </div>
            </div>
          </div>
        </div>
      </div>
   <!-- Features Grid -->
      <div class="grid grid-cols-1 md:grid-cols-3 gap-8 mb-16">
        <div class="bg-black border-2 border-base-300 rounded-xl p-6">
          <div class="w-12 h-12 bg-blue-600 rounded-lg flex items-center justify-center text-white text-xl mb-4">
            🔎
          </div>
          <h3 class="text-lg font-semibold text-white mb-3">Búsqueda Semántica</h3>
          <p class="text-white/80">
            Busca por significado, no solo por palabras clave. La IA entiende el contexto.
          </p>
        </div>

        <div class="bg-black border-2 border-base-300 rounded-xl p-6">
          <div class="w-12 h-12 bg-purple-600 rounded-lg flex items-center justify-center text-white text-xl mb-4">
            📑
          </div>
          <h3 class="text-lg font-semibold text-white mb-3">Múltiples Formatos</h3>
          <p class="text-white/80">
            PDF, Word, Excel, PowerPoint, imágenes escaneadas y más.
          </p>
        </div>

        <div class="bg-black border-2 border-base-300 rounded-xl p-6">
          <div class="w-12 h-12 bg-green-600 rounded-lg flex items-center justify-center text-white text-xl mb-4">
            ⚡
          </div>
          <h3 class="text-lg font-semibold text-white mb-3">Resultados Instantáneos</h3>
          <p class="text-white/80">
            Respuestas en milisegundos, incluso con miles de documentos.
          </p>
        </div>
      </div>
      <!-- Context Box - Moved outside demo -->
     

     
   

      <!-- Stats Section -->
    

      <!-- CTA Section -->
   
    </div>
  </section>
</BaseLayout>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    const searchForm = document.getElementById('doc-search-form') as HTMLFormElement;
    const searchInput = document.getElementById('doc-search-input') as HTMLInputElement;
    const resultsContainer = document.getElementById('doc-results') as HTMLDivElement;
    const quickSearchButtons = document.querySelectorAll('.quick-search-btn');

    if (!searchForm || !searchInput || !resultsContainer) return;

    // Documentos de ejemplo
    const documents = [
      {
        title: 'Política de Teletrabajo 2024',
        category: 'Recursos Humanos',
        excerpt: 'Los empleados pueden trabajar de forma remota hasta 3 días por semana previa aprobación del supervisor directo. Se requiere disponibilidad durante horario laboral y buena conexión a internet.',
        relevance: 95
      },
      {
        title: 'Manual de Procedimientos - Compras',
        category: 'Administración',
        excerpt: 'Las solicitudes de compra superiores a €500 requieren aprobación del departamento de finanzas. Completar formulario FC-102 y adjuntar 3 cotizaciones.',
        relevance: 88
      },
      {
        title: 'Política de Vacaciones',
        category: 'Recursos Humanos',
        excerpt: 'Los empleados tienen derecho a 22 días laborables de vacaciones anuales. Las solicitudes deben presentarse con 15 días de antelación a través del portal de RRHH.',
        relevance: 92
      }
    ];

    function showResults(query: string) {
      resultsContainer.innerHTML = '';

      // Loading state
      resultsContainer.innerHTML = `
        <div class="text-center py-8">
          <div class="inline-flex items-center gap-2 text-white">
            <svg class="animate-spin h-5 w-5" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24">
              <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
              <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path>
            </svg>
            <span>Buscando en 1,247 documentos...</span>
          </div>
        </div>
      `;

      setTimeout(() => {
        const lowerQuery = query.toLowerCase();
        let relevantDocs = documents;

        // Filtrar documentos relevantes
        if (lowerQuery.includes('teletrabajo') || lowerQuery.includes('remoto')) {
          relevantDocs = [documents[0]];
        } else if (lowerQuery.includes('vacaciones')) {
          relevantDocs = [documents[2]];
        } else if (lowerQuery.includes('compra')) {
          relevantDocs = [documents[1]];
        }

        resultsContainer.innerHTML = `
          <div class="bg-gradient-to-br from-blue-500 to-blue-700 rounded-xl p-4 mb-4">
            <div class="flex items-center gap-2 text-sm font-medium text-white">
              <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 20 20">
                <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2v-3a1 1 0 00-1-1H9z" clip-rule="evenodd"></path>
              </svg>
              Encontré ${relevantDocs.length} documento${relevantDocs.length > 1 ? 's' : ''} relevante${relevantDocs.length > 1 ? 's' : ''}
            </div>
          </div>
        `;

        relevantDocs.forEach(doc => {
          const docCard = `
            <div class="bg-white border-2 border-blue-300 rounded-xl p-6 hover:shadow-lg transition-all duration-200" style="color: #000000;">
              <div class="flex items-start justify-between mb-3">
                <div>
                  <h3 class="text-lg font-semibold mb-1" style="color: #000000;">${doc.title}</h3>
                  <span class="inline-flex items-center px-2 py-1 rounded text-xs font-medium bg-blue-100" style="color: #000000;">
                    ${doc.category}
                  </span>
                </div>
                <div class="flex flex-col items-end">
                  <span class="inline-flex items-center px-3 py-1 rounded-lg text-sm font-semibold bg-green-500 text-white shadow-sm">
                    ${doc.relevance}% relevante
                  </span>
                </div>
              </div>
              <p class="mb-4" style="color: #000000;">${doc.excerpt}</p>
              <div class="flex items-center gap-4 text-sm" style="color: #000000;">
                <button class="flex items-center gap-1 hover:opacity-70 transition-colors">
                  <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"></path>
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z"></path>
                  </svg>
                  Ver documento
                </button>
                <button class="flex items-center gap-1 hover:opacity-70 transition-colors">
                  <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 16H6a2 2 0 01-2-2V6a2 2 0 012-2h8a2 2 0 012 2v2m-6 12h8a2 2 0 002-2v-8a2 2 0 00-2-2h-8a2 2 0 00-2 2v8a2 2 0 002 2z"></path>
                  </svg>
                  Copiar extracto
                </button>
              </div>
            </div>
          `;
          resultsContainer.innerHTML += docCard;
        });
      }, 800);
    }

    // Handle form submission
    searchForm.addEventListener('submit', (e) => {
      e.preventDefault();
      const query = searchInput.value.trim();
      if (!query) return;

      showResults(query);
    });

    // Handle quick search buttons
    quickSearchButtons.forEach(button => {
      button.addEventListener('click', () => {
        const query = button.getAttribute('data-query');
        if (query) {
          searchInput.value = query;
          showResults(query);
        }
      });
    });
  });
</script>
