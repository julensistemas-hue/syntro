---
// Meeting request component - Simplified version
---

<section class="py-20 bg-base-25">
  <div class="max-w-5xl mx-auto px-8 md:px-12 lg:px-24">
    <div class="text-center mb-12">
      <h1 class="text-4xl md:text-5xl font-bold text-base-900 mb-6">
        <span class="text-[#0052cc]">Te Llamamos Nosotros</span>
      </h1>
      <p class="text-xl text-base-600 max-w-3xl mx-auto">
        Déjanos tus datos y nuestro equipo se pondrá en contacto contigo en menos de 24 horas
        para analizar cómo podemos ayudarte con IA o ciberseguridad.
      </p>
    </div>

    <!-- Single Wide Form -->
    <div class="max-w-3xl mx-auto">
      <div class="bg-base-50 rounded-2xl p-8 md:p-12 shadow-xl border border-base-200">
        <div id="alert-container" class="mb-6 hidden"></div>

        <form id="meeting-form" class="space-y-6" action="/api/send-meeting-request" method="POST">
          <!-- Nombre -->
          <div>
            <label for="nombre" class="block text-sm font-semibold text-base-900 mb-2">
              Nombre completo *
            </label>
            <input
              type="text"
              id="nombre"
              name="nombre"
              required
              class="w-full px-4 py-3 border border-base-300 rounded-lg focus:ring-2 focus:ring-[#0052cc] focus:border-[#0052cc] bg-base-25 text-base"
              placeholder="Ej: Juan García López"
            />
          </div>

          <!-- Email y Teléfono en una fila -->
          <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
            <div>
              <label for="email" class="block text-sm font-semibold text-base-900 mb-2">
                Email *
              </label>
              <input
                type="email"
                id="email"
                name="email"
                required
                class="w-full px-4 py-3 border border-base-300 rounded-lg focus:ring-2 focus:ring-[#0052cc] focus:border-[#0052cc] bg-base-25 text-base"
                placeholder="tu@empresa.com"
              />
            </div>

            <div>
              <label for="telefono" class="block text-sm font-semibold text-base-900 mb-2">
                Teléfono *
              </label>
              <input
                type="tel"
                id="telefono"
                name="telefono"
                required
                class="w-full px-4 py-3 border border-base-300 rounded-lg focus:ring-2 focus:ring-[#0052cc] focus:border-[#0052cc] bg-base-25 text-base"
                placeholder="+34 600 000 000"
              />
            </div>
          </div>

          <!-- Preferencia de contacto -->
          <div>
            <label for="preferencia_contacto" class="block text-sm font-semibold text-base-900 mb-2">
              ¿Cómo prefieres que te contactemos? *
            </label>
            <select
              id="preferencia_contacto"
              name="preferencia_contacto"
              required
              class="w-full px-4 py-3 border border-base-300 rounded-lg focus:ring-2 focus:ring-[#0052cc] focus:border-[#0052cc] bg-base-25 text-base"
            >
              <option value="">Selecciona una opción</option>
              <option value="llamada">Llamada telefónica</option>
              <option value="whatsapp">WhatsApp</option>
              <option value="email">Email</option>
            </select>
          </div>

          <!-- Mensaje -->
          <div>
            <label for="mensaje" class="block text-sm font-semibold text-base-900 mb-2">
              Cuéntanos qué necesitas *
            </label>
            <textarea
              id="mensaje"
              name="mensaje"
              rows="5"
              required
              class="w-full px-4 py-3 border border-base-300 rounded-lg focus:ring-2 focus:ring-[#0052cc] focus:border-[#0052cc] bg-base-25 text-base"
              placeholder="Ejemplo: Necesito un chatbot para mi tienda online que atienda consultas de clientes sobre pedidos y productos..."
            ></textarea>
          </div>

          <!-- Agendar Reunión Section -->
          <div class="border-t border-base-200 pt-6">
            <div class="bg-blue-50 border border-blue-200 rounded-lg p-4 mb-6">
              <h3 class="text-sm font-semibold text-blue-900 mb-2">📅 ¿Quieres agendar una reunión ahora?</h3>
              <p class="text-sm text-blue-800">
                Selecciona fecha y hora para reservar tu reunión directamente. Te enviaremos un enlace de Google Meet.
              </p>
            </div>

            <!-- Date Selector -->
            <div class="mb-6">
              <label for="selectedDate" class="block text-sm font-semibold text-base-900 mb-2">
                Fecha de la reunión (opcional)
              </label>
              <input
                type="date"
                id="selectedDate"
                name="selectedDate"
                class="w-full px-4 py-3 border border-base-300 rounded-lg focus:ring-2 focus:ring-[#0052cc] focus:border-[#0052cc] bg-base-25 text-base"
              />
              <p class="text-xs text-base-600 mt-2">Todos los días disponibles (incluye fines de semana). Las reuniones dura 1 hora. Si no seleccionas fecha, nos pondremos en contacto contigo para coordinar.</p>
            </div>

            <!-- Time Slots Container -->
            <div id="time-slots-container" class="hidden">
              <label class="block text-sm font-semibold text-base-900 mb-3">
                Hora de la reunión
              </label>
              <div id="time-slots-loading" class="text-center py-4">
                <div class="inline-flex items-center gap-2 text-accent-600">
                  <svg class="animate-spin h-5 w-5" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24">
                    <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
                    <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path>
                  </svg>
                  <span class="text-sm">Verificando disponibilidad...</span>
                </div>
              </div>
              <div id="time-slots-grid" class="hidden grid grid-cols-2 md:grid-cols-4 gap-3">
                <!-- Time slots will be populated here -->
              </div>
              <div id="time-slots-error" class="hidden text-sm text-red-600 bg-red-50 border border-red-200 rounded-lg p-3">
                <!-- Error message will be shown here -->
              </div>
              <input type="hidden" id="selectedTime" name="selectedTime" value="" />
            </div>
          </div>

          <button
            type="submit"
            id="submit-button"
            class="w-full bg-[#0052cc] text-white py-4 px-6 rounded-xl font-semibold text-lg hover:bg-[#003d99] transition-colors duration-200 focus:ring-2 focus:ring-[#0052cc] focus:ring-offset-2 disabled:opacity-50 disabled:cursor-not-allowed shadow-lg"
          >
            <span id="button-text">Enviar Solicitud</span>
            <span id="button-loading" class="hidden">Enviando...</span>
          </button>

          <p class="text-sm text-base-500 text-center">
            Al enviar este formulario, nos autorizas a contactarte sobre nuestros servicios.
          </p>
        </form>
      </div>

      <!-- Info cards below form -->
      <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mt-8">
        <div class="bg-base-50 rounded-xl p-6 text-center border border-base-200">
          <div class="w-12 h-12 bg-green-100 rounded-full flex items-center justify-center mx-auto mb-3">
            <svg class="w-6 h-6 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
            </svg>
          </div>
          <h3 class="font-semibold text-base-900 mb-2">Respuesta Rápida</h3>
          <p class="text-sm text-base-600">Te contactamos en menos de 24 horas</p>
        </div>

        <div class="bg-base-50 rounded-xl p-6 text-center border border-base-200">
          <div class="w-12 h-12 bg-blue-100 rounded-full flex items-center justify-center mx-auto mb-3">
            <svg class="w-6 h-6 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
            </svg>
          </div>
          <h3 class="font-semibold text-base-900 mb-2">Sin Compromiso</h3>
          <p class="text-sm text-base-600">Consulta gratuita sin obligación</p>
        </div>

        <div class="bg-base-50 rounded-xl p-6 text-center border border-base-200">
          <div class="w-12 h-12 bg-purple-100 rounded-full flex items-center justify-center mx-auto mb-3">
            <svg class="w-6 h-6 text-purple-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6V4m0 2a2 2 0 100 4m0-4a2 2 0 110 4m-6 8a2 2 0 100-4m0 4a2 2 0 110-4m0 4v2m0-6V4m6 6v10m6-2a2 2 0 100-4m0 4a2 2 0 110-4m0 4v2m0-6V4"></path>
            </svg>
          </div>
          <h3 class="font-semibold text-base-900 mb-2">Solución a Medida</h3>
          <p class="text-sm text-base-600">Propuesta personalizada para ti</p>
        </div>
      </div>
    </div>
  </div>
</section>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    const form = document.getElementById('meeting-form') as HTMLFormElement;
    const alertContainer = document.getElementById('alert-container') as HTMLDivElement;
    const submitButton = document.getElementById('submit-button') as HTMLButtonElement;
    const buttonText = document.getElementById('button-text') as HTMLSpanElement;
    const buttonLoading = document.getElementById('button-loading') as HTMLSpanElement;

    // Calendar elements
    const dateInput = document.getElementById('selectedDate') as HTMLInputElement;
    const timeSlotsContainer = document.getElementById('time-slots-container') as HTMLDivElement;
    const timeSlotsLoading = document.getElementById('time-slots-loading') as HTMLDivElement;
    const timeSlotsGrid = document.getElementById('time-slots-grid') as HTMLDivElement;
    const timeSlotsError = document.getElementById('time-slots-error') as HTMLDivElement;
    const selectedTimeInput = document.getElementById('selectedTime') as HTMLInputElement;

    // Set min date to tomorrow
    const tomorrow = new Date();
    tomorrow.setDate(tomorrow.getDate() + 1);
    dateInput.min = tomorrow.toISOString().split('T')[0];

    // Set max date to 30 days from now
    const maxDate = new Date();
    maxDate.setDate(maxDate.getDate() + 30);
    dateInput.max = maxDate.toISOString().split('T')[0];

    function showAlert(message: string, type: 'success' | 'error') {
      alertContainer.className = `mb-6 p-4 rounded-lg ${
        type === 'success'
          ? 'bg-green-50 border border-green-200 text-green-800'
          : 'bg-red-50 border border-red-200 text-red-800'
      }`;

      const icon = type === 'success'
        ? '<svg class="w-5 h-5 mr-2 inline" fill="currentColor" viewBox="0 0 20 20"><path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path></svg>'
        : '<svg class="w-5 h-5 mr-2 inline" fill="currentColor" viewBox="0 0 20 20"><path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.707 7.293a1 1 0 00-1.414 1.414L8.586 10l-1.293 1.293a1 1 0 101.414 1.414L10 11.414l1.293 1.293a1 1 0 001.414-1.414L11.414 10l1.293-1.293a1 1 0 00-1.414-1.414L10 8.586 8.707 7.293z" clip-rule="evenodd"></path></svg>';

      alertContainer.innerHTML = `${icon}${message}`;
      alertContainer.classList.remove('hidden');

      // Scroll suave al alert
      alertContainer.scrollIntoView({ behavior: 'smooth', block: 'nearest' });

      // Ocultar después de 5 segundos si es éxito
      if (type === 'success') {
        setTimeout(() => {
          alertContainer.classList.add('hidden');
        }, 5000);
      }
    }

    function setLoading(loading: boolean) {
      submitButton.disabled = loading;
      if (loading) {
        buttonText.classList.add('hidden');
        buttonLoading.classList.remove('hidden');
      } else {
        buttonText.classList.remove('hidden');
        buttonLoading.classList.add('hidden');
      }
    }

    // Fetch available time slots for selected date
    async function fetchAvailableSlots(date: string) {
      timeSlotsContainer.classList.remove('hidden');
      timeSlotsLoading.classList.remove('hidden');
      timeSlotsGrid.classList.add('hidden');
      timeSlotsError.classList.add('hidden');
      selectedTimeInput.value = '';

      try {
        const response = await fetch('/api/check-availability', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ date }),
        });

        const data = await response.json();

        if (!response.ok) {
          throw new Error(data.error || 'Error al verificar disponibilidad');
        }

        timeSlotsLoading.classList.add('hidden');

        if (data.availableSlots.length === 0) {
          timeSlotsError.classList.remove('hidden');
          timeSlotsError.textContent = data.message || 'No hay horarios disponibles para este día. Por favor, selecciona otra fecha.';
          return;
        }

        // Populate time slots
        timeSlotsGrid.innerHTML = '';
        data.availableSlots.forEach((slot: string) => {
          const button = document.createElement('button');
          button.type = 'button';
          button.className = 'px-4 py-3 border-2 border-base-300 rounded-lg hover:border-[#0052cc] hover:bg-blue-50 transition-all duration-200 text-base font-medium text-base-900';
          button.dataset.time = slot;
          button.textContent = `${slot}`;

          button.addEventListener('click', () => {
            // Remove selection from all buttons
            timeSlotsGrid.querySelectorAll('button').forEach(btn => {
              btn.className = 'px-4 py-3 border-2 border-base-300 rounded-lg hover:border-[#0052cc] hover:bg-blue-50 transition-all duration-200 text-base font-medium text-base-900';
            });

            // Add selection to clicked button
            button.className = 'px-4 py-3 border-2 border-[#0052cc] bg-blue-50 rounded-lg text-[#0052cc] font-semibold';
            selectedTimeInput.value = slot;
          });

          timeSlotsGrid.appendChild(button);
        });

        timeSlotsGrid.classList.remove('hidden');
      } catch (error) {
        timeSlotsLoading.classList.add('hidden');
        timeSlotsError.classList.remove('hidden');
        timeSlotsError.textContent = 'Error al verificar disponibilidad. Por favor, inténtalo de nuevo.';
        console.error('Error fetching slots:', error);
      }
    }

    // Handle date selection
    dateInput.addEventListener('change', (e) => {
      const selectedDate = (e.target as HTMLInputElement).value;
      if (selectedDate) {
        fetchAvailableSlots(selectedDate);
      } else {
        timeSlotsContainer.classList.add('hidden');
        selectedTimeInput.value = '';
      }
    });

    form.addEventListener('submit', async (e) => {
      e.preventDefault();

      setLoading(true);
      alertContainer.classList.add('hidden');

      try {
        const formData = new FormData(form);

        const response = await fetch('/api/send-meeting-request', {
          method: 'POST',
          body: formData,
        });

        const data = await response.json();

        if (response.ok) {
          let successMessage = data.meetLink
            ? `¡Reunión agendada! Te hemos enviado un email con el enlace de Google Meet. <br><a href="${data.meetLink}" target="_blank" class="underline font-semibold">Abrir Google Meet</a>`
            : '¡Solicitud enviada! Nos pondremos en contacto contigo en menos de 24 horas.';

          showAlert(successMessage, 'success');
          form.reset();
          timeSlotsContainer.classList.add('hidden');
        } else {
          showAlert(
            data.error || 'Ha ocurrido un error. Por favor, inténtalo de nuevo.',
            'error'
          );
        }
      } catch (error) {
        console.error('Error:', error);
        showAlert(
          'Error de conexión. Por favor, verifica tu conexión e inténtalo de nuevo.',
          'error'
        );
      } finally {
        setLoading(false);
      }
    });
  });
</script>